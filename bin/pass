#!/usr/bin/env ruby

require 'rubygems'
require 'pass'

num = 12
num_times = 1

if ARGV[0]
  num_times = ARGV[0].to_i
end

if ARGV[1]
  num = ARGV[1].to_i
end

num_times.times{
  puts Pass.generate num
}
